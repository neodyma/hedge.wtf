/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';

export type FaucetMint = Account<FaucetMintAccountData>;

export type FaucetMintAccountData = {
  discriminator: Uint8Array;
  market: PublicKey;
  mint: PublicKey;
  mintAuthorityBump: number;
  decimals: number;
  bump: number;
};

export type FaucetMintAccountDataArgs = {
  market: PublicKey;
  mint: PublicKey;
  mintAuthorityBump: number;
  decimals: number;
  bump: number;
};

export function getFaucetMintAccountDataSerializer(): Serializer<
  FaucetMintAccountDataArgs,
  FaucetMintAccountData
> {
  return mapSerializer<FaucetMintAccountDataArgs, any, FaucetMintAccountData>(
    struct<FaucetMintAccountData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['market', publicKeySerializer()],
        ['mint', publicKeySerializer()],
        ['mintAuthorityBump', u8()],
        ['decimals', u8()],
        ['bump', u8()],
      ],
      { description: 'FaucetMintAccountData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([52, 242, 34, 51, 144, 220, 134, 52]),
    })
  ) as Serializer<FaucetMintAccountDataArgs, FaucetMintAccountData>;
}

export function deserializeFaucetMint(rawAccount: RpcAccount): FaucetMint {
  return deserializeAccount(rawAccount, getFaucetMintAccountDataSerializer());
}

export async function fetchFaucetMint(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<FaucetMint> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'FaucetMint');
  return deserializeFaucetMint(maybeAccount);
}

export async function safeFetchFaucetMint(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<FaucetMint | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeFaucetMint(maybeAccount) : null;
}

export async function fetchAllFaucetMint(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<FaucetMint[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'FaucetMint');
    return deserializeFaucetMint(maybeAccount);
  });
}

export async function safeFetchAllFaucetMint(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<FaucetMint[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) => deserializeFaucetMint(maybeAccount as RpcAccount));
}

export function getFaucetMintGpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'zodialV2',
    '5E1ikr753b8RQZdtohZAY8wmpjn2hu9dWzrN5xEasmtu'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      discriminator: Uint8Array;
      market: PublicKey;
      mint: PublicKey;
      mintAuthorityBump: number;
      decimals: number;
      bump: number;
    }>({
      discriminator: [0, bytes({ size: 8 })],
      market: [8, publicKeySerializer()],
      mint: [40, publicKeySerializer()],
      mintAuthorityBump: [72, u8()],
      decimals: [73, u8()],
      bump: [74, u8()],
    })
    .deserializeUsing<FaucetMint>((account) => deserializeFaucetMint(account))
    .whereField(
      'discriminator',
      new Uint8Array([52, 242, 34, 51, 144, 220, 134, 52])
    );
}

export function getFaucetMintSize(): number {
  return 75;
}

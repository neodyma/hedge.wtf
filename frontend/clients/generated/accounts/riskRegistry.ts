/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Account,
  Context,
  Pda,
  PublicKey,
  RpcAccount,
  RpcGetAccountOptions,
  RpcGetAccountsOptions,
  assertAccountExists,
  deserializeAccount,
  gpaBuilder,
  publicKey as toPublicKey,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u16,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { RiskPair, RiskPairArgs, getRiskPairSerializer } from '../types';

export type RiskRegistry = Account<RiskRegistryAccountData>;

export type RiskRegistryAccountData = {
  discriminator: Uint8Array;
  market: PublicKey;
  bump: number;
  dim: number;
  pairs: Array<RiskPair>;
};

export type RiskRegistryAccountDataArgs = {
  market: PublicKey;
  bump: number;
  dim: number;
  pairs: Array<RiskPairArgs>;
};

export function getRiskRegistryAccountDataSerializer(): Serializer<
  RiskRegistryAccountDataArgs,
  RiskRegistryAccountData
> {
  return mapSerializer<
    RiskRegistryAccountDataArgs,
    any,
    RiskRegistryAccountData
  >(
    struct<RiskRegistryAccountData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['market', publicKeySerializer()],
        ['bump', u8()],
        ['dim', u16()],
        ['pairs', array(getRiskPairSerializer())],
      ],
      { description: 'RiskRegistryAccountData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([54, 31, 141, 146, 91, 210, 66, 46]),
    })
  ) as Serializer<RiskRegistryAccountDataArgs, RiskRegistryAccountData>;
}

export function deserializeRiskRegistry(rawAccount: RpcAccount): RiskRegistry {
  return deserializeAccount(rawAccount, getRiskRegistryAccountDataSerializer());
}

export async function fetchRiskRegistry(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<RiskRegistry> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  assertAccountExists(maybeAccount, 'RiskRegistry');
  return deserializeRiskRegistry(maybeAccount);
}

export async function safeFetchRiskRegistry(
  context: Pick<Context, 'rpc'>,
  publicKey: PublicKey | Pda,
  options?: RpcGetAccountOptions
): Promise<RiskRegistry | null> {
  const maybeAccount = await context.rpc.getAccount(
    toPublicKey(publicKey, false),
    options
  );
  return maybeAccount.exists ? deserializeRiskRegistry(maybeAccount) : null;
}

export async function fetchAllRiskRegistry(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<RiskRegistry[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts.map((maybeAccount) => {
    assertAccountExists(maybeAccount, 'RiskRegistry');
    return deserializeRiskRegistry(maybeAccount);
  });
}

export async function safeFetchAllRiskRegistry(
  context: Pick<Context, 'rpc'>,
  publicKeys: Array<PublicKey | Pda>,
  options?: RpcGetAccountsOptions
): Promise<RiskRegistry[]> {
  const maybeAccounts = await context.rpc.getAccounts(
    publicKeys.map((key) => toPublicKey(key, false)),
    options
  );
  return maybeAccounts
    .filter((maybeAccount) => maybeAccount.exists)
    .map((maybeAccount) => deserializeRiskRegistry(maybeAccount as RpcAccount));
}

export function getRiskRegistryGpaBuilder(
  context: Pick<Context, 'rpc' | 'programs'>
) {
  const programId = context.programs.getPublicKey(
    'zodialV2',
    '5E1ikr753b8RQZdtohZAY8wmpjn2hu9dWzrN5xEasmtu'
  );
  return gpaBuilder(context, programId)
    .registerFields<{
      discriminator: Uint8Array;
      market: PublicKey;
      bump: number;
      dim: number;
      pairs: Array<RiskPairArgs>;
    }>({
      discriminator: [0, bytes({ size: 8 })],
      market: [8, publicKeySerializer()],
      bump: [40, u8()],
      dim: [41, u16()],
      pairs: [43, array(getRiskPairSerializer())],
    })
    .deserializeUsing<RiskRegistry>((account) =>
      deserializeRiskRegistry(account)
    )
    .whereField(
      'discriminator',
      new Uint8Array([54, 31, 141, 146, 91, 210, 66, 46])
    );
}

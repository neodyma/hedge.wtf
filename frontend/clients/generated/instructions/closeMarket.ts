/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type CloseMarketInstructionAccounts = {
  market?: PublicKey | Pda;
  authority?: Signer;
  /** Receiver of the reclaimed rent */
  receiver: PublicKey | Pda;
};

// Data.
export type CloseMarketInstructionData = { discriminator: Uint8Array };

export type CloseMarketInstructionDataArgs = {};

export function getCloseMarketInstructionDataSerializer(): Serializer<
  CloseMarketInstructionDataArgs,
  CloseMarketInstructionData
> {
  return mapSerializer<
    CloseMarketInstructionDataArgs,
    any,
    CloseMarketInstructionData
  >(
    struct<CloseMarketInstructionData>(
      [['discriminator', bytes({ size: 8 })]],
      { description: 'CloseMarketInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([88, 154, 248, 186, 48, 14, 123, 244]),
    })
  ) as Serializer<CloseMarketInstructionDataArgs, CloseMarketInstructionData>;
}

// Instruction.
export function closeMarket(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: CloseMarketInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'zodialV2',
    '5E1ikr753b8RQZdtohZAY8wmpjn2hu9dWzrN5xEasmtu'
  );

  // Accounts.
  const resolvedAccounts = {
    market: {
      index: 0,
      isWritable: true as boolean,
      value: input.market ?? null,
    },
    authority: {
      index: 1,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    receiver: {
      index: 2,
      isWritable: true as boolean,
      value: input.receiver ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.market.value) {
    resolvedAccounts.market.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([109, 97, 114, 107, 101, 116])),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.authority.value)
      ),
    ]);
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCloseMarketInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}

/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type CloseObligationInstructionAccounts = {
  /** Either the obligation owner OR the market authority can close */
  signer: Signer;
  market: PublicKey | Pda;
  authority?: PublicKey | Pda;
  obligation?: PublicKey | Pda;
  owner: PublicKey | Pda;
  /** Receiver of the reclaimed rent */
  receiver: PublicKey | Pda;
};

// Data.
export type CloseObligationInstructionData = { discriminator: Uint8Array };

export type CloseObligationInstructionDataArgs = {};

export function getCloseObligationInstructionDataSerializer(): Serializer<
  CloseObligationInstructionDataArgs,
  CloseObligationInstructionData
> {
  return mapSerializer<
    CloseObligationInstructionDataArgs,
    any,
    CloseObligationInstructionData
  >(
    struct<CloseObligationInstructionData>(
      [['discriminator', bytes({ size: 8 })]],
      { description: 'CloseObligationInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([178, 182, 18, 237, 158, 158, 51, 124]),
    })
  ) as Serializer<
    CloseObligationInstructionDataArgs,
    CloseObligationInstructionData
  >;
}

// Instruction.
export function closeObligation(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: CloseObligationInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'zodialV2',
    '5E1ikr753b8RQZdtohZAY8wmpjn2hu9dWzrN5xEasmtu'
  );

  // Accounts.
  const resolvedAccounts = {
    signer: {
      index: 0,
      isWritable: true as boolean,
      value: input.signer ?? null,
    },
    market: {
      index: 1,
      isWritable: false as boolean,
      value: input.market ?? null,
    },
    authority: {
      index: 2,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    obligation: {
      index: 3,
      isWritable: true as boolean,
      value: input.obligation ?? null,
    },
    owner: {
      index: 4,
      isWritable: false as boolean,
      value: input.owner ?? null,
    },
    receiver: {
      index: 5,
      isWritable: true as boolean,
      value: input.receiver ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity.publicKey;
  }
  if (!resolvedAccounts.obligation.value) {
    resolvedAccounts.obligation.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([111, 98, 108, 105, 103, 97, 116, 105, 111, 110])
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.market.value)
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.owner.value)
      ),
    ]);
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCloseObligationInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}

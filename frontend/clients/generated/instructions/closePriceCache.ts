/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type ClosePriceCacheInstructionAccounts = {
  market: PublicKey | Pda;
  authority?: Signer;
  priceCache?: PublicKey | Pda;
  /** Receiver of the reclaimed rent */
  receiver: PublicKey | Pda;
};

// Data.
export type ClosePriceCacheInstructionData = { discriminator: Uint8Array };

export type ClosePriceCacheInstructionDataArgs = {};

export function getClosePriceCacheInstructionDataSerializer(): Serializer<
  ClosePriceCacheInstructionDataArgs,
  ClosePriceCacheInstructionData
> {
  return mapSerializer<
    ClosePriceCacheInstructionDataArgs,
    any,
    ClosePriceCacheInstructionData
  >(
    struct<ClosePriceCacheInstructionData>(
      [['discriminator', bytes({ size: 8 })]],
      { description: 'ClosePriceCacheInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([67, 133, 139, 80, 128, 226, 100, 62]),
    })
  ) as Serializer<
    ClosePriceCacheInstructionDataArgs,
    ClosePriceCacheInstructionData
  >;
}

// Instruction.
export function closePriceCache(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: ClosePriceCacheInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'zodialV2',
    '5E1ikr753b8RQZdtohZAY8wmpjn2hu9dWzrN5xEasmtu'
  );

  // Accounts.
  const resolvedAccounts = {
    market: {
      index: 0,
      isWritable: true as boolean,
      value: input.market ?? null,
    },
    authority: {
      index: 1,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    priceCache: {
      index: 2,
      isWritable: true as boolean,
      value: input.priceCache ?? null,
    },
    receiver: {
      index: 3,
      isWritable: true as boolean,
      value: input.receiver ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.priceCache.value) {
    resolvedAccounts.priceCache.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([112, 114, 105, 99, 101, 45, 99, 97, 99, 104, 101])
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.market.value)
      ),
    ]);
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getClosePriceCacheInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}

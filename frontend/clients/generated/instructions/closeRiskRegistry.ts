/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type CloseRiskRegistryInstructionAccounts = {
  market: PublicKey | Pda;
  authority?: Signer;
  riskRegistry?: PublicKey | Pda;
  /** Receiver of the reclaimed rent */
  receiver: PublicKey | Pda;
};

// Data.
export type CloseRiskRegistryInstructionData = { discriminator: Uint8Array };

export type CloseRiskRegistryInstructionDataArgs = {};

export function getCloseRiskRegistryInstructionDataSerializer(): Serializer<
  CloseRiskRegistryInstructionDataArgs,
  CloseRiskRegistryInstructionData
> {
  return mapSerializer<
    CloseRiskRegistryInstructionDataArgs,
    any,
    CloseRiskRegistryInstructionData
  >(
    struct<CloseRiskRegistryInstructionData>(
      [['discriminator', bytes({ size: 8 })]],
      { description: 'CloseRiskRegistryInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([68, 90, 188, 132, 5, 197, 51, 133]),
    })
  ) as Serializer<
    CloseRiskRegistryInstructionDataArgs,
    CloseRiskRegistryInstructionData
  >;
}

// Instruction.
export function closeRiskRegistry(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: CloseRiskRegistryInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'zodialV2',
    '5E1ikr753b8RQZdtohZAY8wmpjn2hu9dWzrN5xEasmtu'
  );

  // Accounts.
  const resolvedAccounts = {
    market: {
      index: 0,
      isWritable: true as boolean,
      value: input.market ?? null,
    },
    authority: {
      index: 1,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    riskRegistry: {
      index: 2,
      isWritable: true as boolean,
      value: input.riskRegistry ?? null,
    },
    receiver: {
      index: 3,
      isWritable: true as boolean,
      value: input.receiver ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.riskRegistry.value) {
    resolvedAccounts.riskRegistry.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([114, 105, 115, 107, 45, 114, 101, 103])
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.market.value)
      ),
    ]);
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCloseRiskRegistryInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}

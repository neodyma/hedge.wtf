/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u16,
  u64,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';
import { PriceMode, PriceModeArgs, getPriceModeSerializer } from '../types';

// Accounts.
export type InitMarketInstructionAccounts = {
  payer?: Signer;
  authority?: Signer;
  market?: PublicKey | Pda;
  assetRegistry?: PublicKey | Pda;
  riskRegistry?: PublicKey | Pda;
  priceCache?: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type InitMarketInstructionData = {
  discriminator: Uint8Array;
  maxAssets: number;
  maxPositions: number;
  defaultLtvBps: number;
  defaultLiqThresholdBps: number;
  defaultLiqBonusBps: number;
  priceMode: PriceMode;
  pythMaxAgeSecs: bigint;
};

export type InitMarketInstructionDataArgs = {
  maxAssets: number;
  maxPositions: number;
  defaultLtvBps: number;
  defaultLiqThresholdBps: number;
  defaultLiqBonusBps: number;
  priceMode: PriceModeArgs;
  pythMaxAgeSecs: number | bigint;
};

export function getInitMarketInstructionDataSerializer(): Serializer<
  InitMarketInstructionDataArgs,
  InitMarketInstructionData
> {
  return mapSerializer<
    InitMarketInstructionDataArgs,
    any,
    InitMarketInstructionData
  >(
    struct<InitMarketInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['maxAssets', u16()],
        ['maxPositions', u16()],
        ['defaultLtvBps', u16()],
        ['defaultLiqThresholdBps', u16()],
        ['defaultLiqBonusBps', u16()],
        ['priceMode', getPriceModeSerializer()],
        ['pythMaxAgeSecs', u64()],
      ],
      { description: 'InitMarketInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([33, 253, 15, 116, 89, 25, 127, 236]),
    })
  ) as Serializer<InitMarketInstructionDataArgs, InitMarketInstructionData>;
}

// Args.
export type InitMarketInstructionArgs = InitMarketInstructionDataArgs;

// Instruction.
export function initMarket(
  context: Pick<Context, 'eddsa' | 'identity' | 'payer' | 'programs'>,
  input: InitMarketInstructionAccounts & InitMarketInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'zodialV2',
    '5E1ikr753b8RQZdtohZAY8wmpjn2hu9dWzrN5xEasmtu'
  );

  // Accounts.
  const resolvedAccounts = {
    payer: {
      index: 0,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    authority: {
      index: 1,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    market: {
      index: 2,
      isWritable: true as boolean,
      value: input.market ?? null,
    },
    assetRegistry: {
      index: 3,
      isWritable: true as boolean,
      value: input.assetRegistry ?? null,
    },
    riskRegistry: {
      index: 4,
      isWritable: true as boolean,
      value: input.riskRegistry ?? null,
    },
    priceCache: {
      index: 5,
      isWritable: true as boolean,
      value: input.priceCache ?? null,
    },
    systemProgram: {
      index: 6,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitMarketInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.market.value) {
    resolvedAccounts.market.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([109, 97, 114, 107, 101, 116])),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.authority.value)
      ),
    ]);
  }
  if (!resolvedAccounts.assetRegistry.value) {
    resolvedAccounts.assetRegistry.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([97, 115, 115, 101, 116, 45, 114, 101, 103])
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.market.value)
      ),
    ]);
  }
  if (!resolvedAccounts.riskRegistry.value) {
    resolvedAccounts.riskRegistry.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([114, 105, 115, 107, 45, 114, 101, 103])
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.market.value)
      ),
    ]);
  }
  if (!resolvedAccounts.priceCache.value) {
    resolvedAccounts.priceCache.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([112, 114, 105, 99, 101, 45, 99, 97, 99, 104, 101])
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.market.value)
      ),
    ]);
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'systemProgram',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInitMarketInstructionDataSerializer().serialize(
    resolvedArgs as InitMarketInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}

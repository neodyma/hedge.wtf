/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';
import { RateModel, RateModelArgs, getRateModelSerializer } from '../types';

// Accounts.
export type InitPoolInstructionAccounts = {
  payer?: Signer;
  /** Admin = market.authority */
  authority?: Signer;
  market?: PublicKey | Pda;
  assetRegistry?: PublicKey | Pda;
  pool: PublicKey | Pda;
  mint: PublicKey | Pda;
  vaultAuth?: PublicKey | Pda;
  vault?: PublicKey | Pda;
  tokenProgram?: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type InitPoolInstructionData = {
  discriminator: Uint8Array;
  rate: RateModel;
  mint: PublicKey;
};

export type InitPoolInstructionDataArgs = {
  rate: RateModelArgs;
  mint: PublicKey;
};

export function getInitPoolInstructionDataSerializer(): Serializer<
  InitPoolInstructionDataArgs,
  InitPoolInstructionData
> {
  return mapSerializer<
    InitPoolInstructionDataArgs,
    any,
    InitPoolInstructionData
  >(
    struct<InitPoolInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['rate', getRateModelSerializer()],
        ['mint', publicKeySerializer()],
      ],
      { description: 'InitPoolInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([116, 233, 199, 204, 115, 159, 171, 36]),
    })
  ) as Serializer<InitPoolInstructionDataArgs, InitPoolInstructionData>;
}

// Args.
export type InitPoolInstructionArgs = InitPoolInstructionDataArgs;

// Instruction.
export function initPool(
  context: Pick<Context, 'eddsa' | 'identity' | 'payer' | 'programs'>,
  accounts: InitPoolInstructionAccounts,
  args: InitPoolInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'zodialV2',
    '5E1ikr753b8RQZdtohZAY8wmpjn2hu9dWzrN5xEasmtu'
  );

  // Accounts.
  const resolvedAccounts = {
    payer: {
      index: 0,
      isWritable: true as boolean,
      value: accounts.payer ?? null,
    },
    authority: {
      index: 1,
      isWritable: false as boolean,
      value: accounts.authority ?? null,
    },
    market: {
      index: 2,
      isWritable: false as boolean,
      value: accounts.market ?? null,
    },
    assetRegistry: {
      index: 3,
      isWritable: false as boolean,
      value: accounts.assetRegistry ?? null,
    },
    pool: {
      index: 4,
      isWritable: true as boolean,
      value: accounts.pool ?? null,
    },
    mint: {
      index: 5,
      isWritable: false as boolean,
      value: accounts.mint ?? null,
    },
    vaultAuth: {
      index: 6,
      isWritable: false as boolean,
      value: accounts.vaultAuth ?? null,
    },
    vault: {
      index: 7,
      isWritable: true as boolean,
      value: accounts.vault ?? null,
    },
    tokenProgram: {
      index: 8,
      isWritable: false as boolean,
      value: accounts.tokenProgram ?? null,
    },
    systemProgram: {
      index: 9,
      isWritable: false as boolean,
      value: accounts.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: InitPoolInstructionArgs = { ...args };

  // Default values.
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.market.value) {
    resolvedAccounts.market.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([109, 97, 114, 107, 101, 116])),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.authority.value)
      ),
    ]);
  }
  if (!resolvedAccounts.assetRegistry.value) {
    resolvedAccounts.assetRegistry.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([97, 115, 115, 101, 116, 45, 114, 101, 103])
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.market.value)
      ),
    ]);
  }
  if (!resolvedAccounts.vaultAuth.value) {
    resolvedAccounts.vaultAuth.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([118, 97, 117, 108, 116, 45, 97, 117, 116, 104])
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.pool.value)
      ),
    ]);
  }
  if (!resolvedAccounts.vault.value) {
    resolvedAccounts.vault.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([118, 97, 117, 108, 116])),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.pool.value)
      ),
    ]);
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'tokenProgram',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'systemProgram',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getInitPoolInstructionDataSerializer().serialize(
    resolvedArgs as InitPoolInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}

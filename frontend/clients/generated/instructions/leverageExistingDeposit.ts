/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u64,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type LeverageExistingDepositInstructionAccounts = {
  owner: Signer;
  market: PublicKey | Pda;
  authority?: PublicKey | Pda;
  assetRegistry?: PublicKey | Pda;
  riskRegistry?: PublicKey | Pda;
  priceCache?: PublicKey | Pda;
  borrowPool: PublicKey | Pda;
  depositPool: PublicKey | Pda;
  obligation?: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type LeverageExistingDepositInstructionData = {
  discriminator: Uint8Array;
  borrowAmount: bigint;
};

export type LeverageExistingDepositInstructionDataArgs = {
  borrowAmount: number | bigint;
};

export function getLeverageExistingDepositInstructionDataSerializer(): Serializer<
  LeverageExistingDepositInstructionDataArgs,
  LeverageExistingDepositInstructionData
> {
  return mapSerializer<
    LeverageExistingDepositInstructionDataArgs,
    any,
    LeverageExistingDepositInstructionData
  >(
    struct<LeverageExistingDepositInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['borrowAmount', u64()],
      ],
      { description: 'LeverageExistingDepositInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([118, 89, 74, 172, 154, 171, 224, 18]),
    })
  ) as Serializer<
    LeverageExistingDepositInstructionDataArgs,
    LeverageExistingDepositInstructionData
  >;
}

// Args.
export type LeverageExistingDepositInstructionArgs =
  LeverageExistingDepositInstructionDataArgs;

// Instruction.
export function leverageExistingDeposit(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: LeverageExistingDepositInstructionAccounts &
    LeverageExistingDepositInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'zodialV2',
    '5E1ikr753b8RQZdtohZAY8wmpjn2hu9dWzrN5xEasmtu'
  );

  // Accounts.
  const resolvedAccounts = {
    owner: {
      index: 0,
      isWritable: true as boolean,
      value: input.owner ?? null,
    },
    market: {
      index: 1,
      isWritable: false as boolean,
      value: input.market ?? null,
    },
    authority: {
      index: 2,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    assetRegistry: {
      index: 3,
      isWritable: false as boolean,
      value: input.assetRegistry ?? null,
    },
    riskRegistry: {
      index: 4,
      isWritable: false as boolean,
      value: input.riskRegistry ?? null,
    },
    priceCache: {
      index: 5,
      isWritable: false as boolean,
      value: input.priceCache ?? null,
    },
    borrowPool: {
      index: 6,
      isWritable: true as boolean,
      value: input.borrowPool ?? null,
    },
    depositPool: {
      index: 7,
      isWritable: true as boolean,
      value: input.depositPool ?? null,
    },
    obligation: {
      index: 8,
      isWritable: true as boolean,
      value: input.obligation ?? null,
    },
    systemProgram: {
      index: 9,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: LeverageExistingDepositInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity.publicKey;
  }
  if (!resolvedAccounts.assetRegistry.value) {
    resolvedAccounts.assetRegistry.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([97, 115, 115, 101, 116, 45, 114, 101, 103])
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.market.value)
      ),
    ]);
  }
  if (!resolvedAccounts.riskRegistry.value) {
    resolvedAccounts.riskRegistry.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([114, 105, 115, 107, 45, 114, 101, 103])
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.market.value)
      ),
    ]);
  }
  if (!resolvedAccounts.priceCache.value) {
    resolvedAccounts.priceCache.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([112, 114, 105, 99, 101, 45, 99, 97, 99, 104, 101])
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.market.value)
      ),
    ]);
  }
  if (!resolvedAccounts.obligation.value) {
    resolvedAccounts.obligation.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([111, 98, 108, 105, 103, 97, 116, 105, 111, 110])
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.market.value)
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.owner.value)
      ),
    ]);
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'systemProgram',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getLeverageExistingDepositInstructionDataSerializer().serialize(
    resolvedArgs as LeverageExistingDepositInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}

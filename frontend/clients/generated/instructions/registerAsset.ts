/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Option,
  OptionOrNullable,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bool,
  bytes,
  mapSerializer,
  option,
  publicKey as publicKeySerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type RegisterAssetInstructionAccounts = {
  authority?: Signer;
  market?: PublicKey | Pda;
  assetRegistry?: PublicKey | Pda;
  riskRegistry?: PublicKey | Pda;
  systemProgram?: PublicKey | Pda;
};

// Data.
export type RegisterAssetInstructionData = {
  discriminator: Uint8Array;
  mint: PublicKey;
  decimals: number;
  pythPrice: PublicKey;
  pythFeedId: Option<Uint8Array>;
  enabledAsCollateral: boolean;
};

export type RegisterAssetInstructionDataArgs = {
  mint: PublicKey;
  decimals: number;
  pythPrice: PublicKey;
  pythFeedId: OptionOrNullable<Uint8Array>;
  enabledAsCollateral: boolean;
};

export function getRegisterAssetInstructionDataSerializer(): Serializer<
  RegisterAssetInstructionDataArgs,
  RegisterAssetInstructionData
> {
  return mapSerializer<
    RegisterAssetInstructionDataArgs,
    any,
    RegisterAssetInstructionData
  >(
    struct<RegisterAssetInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['mint', publicKeySerializer()],
        ['decimals', u8()],
        ['pythPrice', publicKeySerializer()],
        ['pythFeedId', option(bytes({ size: 66 }))],
        ['enabledAsCollateral', bool()],
      ],
      { description: 'RegisterAssetInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([21, 80, 155, 149, 117, 207, 235, 16]),
    })
  ) as Serializer<
    RegisterAssetInstructionDataArgs,
    RegisterAssetInstructionData
  >;
}

// Args.
export type RegisterAssetInstructionArgs = RegisterAssetInstructionDataArgs;

// Instruction.
export function registerAsset(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: RegisterAssetInstructionAccounts & RegisterAssetInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'zodialV2',
    '5E1ikr753b8RQZdtohZAY8wmpjn2hu9dWzrN5xEasmtu'
  );

  // Accounts.
  const resolvedAccounts = {
    authority: {
      index: 0,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    market: {
      index: 1,
      isWritable: true as boolean,
      value: input.market ?? null,
    },
    assetRegistry: {
      index: 2,
      isWritable: true as boolean,
      value: input.assetRegistry ?? null,
    },
    riskRegistry: {
      index: 3,
      isWritable: true as boolean,
      value: input.riskRegistry ?? null,
    },
    systemProgram: {
      index: 4,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: RegisterAssetInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.market.value) {
    resolvedAccounts.market.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([109, 97, 114, 107, 101, 116])),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.authority.value)
      ),
    ]);
  }
  if (!resolvedAccounts.assetRegistry.value) {
    resolvedAccounts.assetRegistry.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([97, 115, 115, 101, 116, 45, 114, 101, 103])
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.market.value)
      ),
    ]);
  }
  if (!resolvedAccounts.riskRegistry.value) {
    resolvedAccounts.riskRegistry.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([114, 105, 115, 107, 45, 114, 101, 103])
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.market.value)
      ),
    ]);
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'systemProgram',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getRegisterAssetInstructionDataSerializer().serialize(
    resolvedArgs as RegisterAssetInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}

/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
  u16,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type SetRiskPairInstructionAccounts = {
  authority?: Signer;
  market?: PublicKey | Pda;
  assetRegistry?: PublicKey | Pda;
  riskRegistry?: PublicKey | Pda;
};

// Data.
export type SetRiskPairInstructionData = {
  discriminator: Uint8Array;
  aMint: PublicKey;
  bMint: PublicKey;
  ltvBps: number;
  liqThresholdBps: number;
  liqBonusBps: number;
};

export type SetRiskPairInstructionDataArgs = {
  aMint: PublicKey;
  bMint: PublicKey;
  ltvBps: number;
  liqThresholdBps: number;
  liqBonusBps: number;
};

export function getSetRiskPairInstructionDataSerializer(): Serializer<
  SetRiskPairInstructionDataArgs,
  SetRiskPairInstructionData
> {
  return mapSerializer<
    SetRiskPairInstructionDataArgs,
    any,
    SetRiskPairInstructionData
  >(
    struct<SetRiskPairInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['aMint', publicKeySerializer()],
        ['bMint', publicKeySerializer()],
        ['ltvBps', u16()],
        ['liqThresholdBps', u16()],
        ['liqBonusBps', u16()],
      ],
      { description: 'SetRiskPairInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([249, 101, 197, 160, 16, 180, 80, 2]),
    })
  ) as Serializer<SetRiskPairInstructionDataArgs, SetRiskPairInstructionData>;
}

// Args.
export type SetRiskPairInstructionArgs = SetRiskPairInstructionDataArgs;

// Instruction.
export function setRiskPair(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: SetRiskPairInstructionAccounts & SetRiskPairInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'zodialV2',
    '5E1ikr753b8RQZdtohZAY8wmpjn2hu9dWzrN5xEasmtu'
  );

  // Accounts.
  const resolvedAccounts = {
    authority: {
      index: 0,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    market: {
      index: 1,
      isWritable: true as boolean,
      value: input.market ?? null,
    },
    assetRegistry: {
      index: 2,
      isWritable: false as boolean,
      value: input.assetRegistry ?? null,
    },
    riskRegistry: {
      index: 3,
      isWritable: true as boolean,
      value: input.riskRegistry ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: SetRiskPairInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.market.value) {
    resolvedAccounts.market.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([109, 97, 114, 107, 101, 116])),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.authority.value)
      ),
    ]);
  }
  if (!resolvedAccounts.assetRegistry.value) {
    resolvedAccounts.assetRegistry.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([97, 115, 115, 101, 116, 45, 114, 101, 103])
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.market.value)
      ),
    ]);
  }
  if (!resolvedAccounts.riskRegistry.value) {
    resolvedAccounts.riskRegistry.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([114, 105, 115, 107, 45, 114, 101, 103])
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.market.value)
      ),
    ]);
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getSetRiskPairInstructionDataSerializer().serialize(
    resolvedArgs as SetRiskPairInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}

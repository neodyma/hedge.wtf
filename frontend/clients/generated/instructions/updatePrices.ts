/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';
import {
  PriceUpdate,
  PriceUpdateArgs,
  getPriceUpdateSerializer,
} from '../types';

// Accounts.
export type UpdatePricesInstructionAccounts = {
  authority?: Signer;
  market?: PublicKey | Pda;
  assetRegistry?: PublicKey | Pda;
  priceCache?: PublicKey | Pda;
};

// Data.
export type UpdatePricesInstructionData = {
  discriminator: Uint8Array;
  updates: Array<PriceUpdate>;
};

export type UpdatePricesInstructionDataArgs = {
  updates: Array<PriceUpdateArgs>;
};

export function getUpdatePricesInstructionDataSerializer(): Serializer<
  UpdatePricesInstructionDataArgs,
  UpdatePricesInstructionData
> {
  return mapSerializer<
    UpdatePricesInstructionDataArgs,
    any,
    UpdatePricesInstructionData
  >(
    struct<UpdatePricesInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['updates', array(getPriceUpdateSerializer())],
      ],
      { description: 'UpdatePricesInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([62, 161, 234, 136, 106, 26, 18, 160]),
    })
  ) as Serializer<UpdatePricesInstructionDataArgs, UpdatePricesInstructionData>;
}

// Args.
export type UpdatePricesInstructionArgs = UpdatePricesInstructionDataArgs;

// Instruction.
export function updatePrices(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: UpdatePricesInstructionAccounts & UpdatePricesInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'zodialV2',
    '5E1ikr753b8RQZdtohZAY8wmpjn2hu9dWzrN5xEasmtu'
  );

  // Accounts.
  const resolvedAccounts = {
    authority: {
      index: 0,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    market: {
      index: 1,
      isWritable: true as boolean,
      value: input.market ?? null,
    },
    assetRegistry: {
      index: 2,
      isWritable: false as boolean,
      value: input.assetRegistry ?? null,
    },
    priceCache: {
      index: 3,
      isWritable: true as boolean,
      value: input.priceCache ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpdatePricesInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.market.value) {
    resolvedAccounts.market.value = context.eddsa.findPda(programId, [
      bytes().serialize(new Uint8Array([109, 97, 114, 107, 101, 116])),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.authority.value)
      ),
    ]);
  }
  if (!resolvedAccounts.assetRegistry.value) {
    resolvedAccounts.assetRegistry.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([97, 115, 115, 101, 116, 45, 114, 101, 103])
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.market.value)
      ),
    ]);
  }
  if (!resolvedAccounts.priceCache.value) {
    resolvedAccounts.priceCache.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([112, 114, 105, 99, 101, 45, 99, 97, 99, 104, 101])
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.market.value)
      ),
    ]);
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getUpdatePricesInstructionDataSerializer().serialize(
    resolvedArgs as UpdatePricesInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}

/**
 * This code was AUTOGENERATED using the codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  Context,
  Pda,
  PublicKey,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  bytes,
  mapSerializer,
  publicKey as publicKeySerializer,
  struct,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type UpdatePricesPythInstructionAccounts = {
  market: PublicKey | Pda;
  assetRegistry?: PublicKey | Pda;
  priceCache?: PublicKey | Pda;
  /** The price update account from Pyth (contains multiple feeds) */
  priceUpdate: PublicKey | Pda;
};

// Data.
export type UpdatePricesPythInstructionData = {
  discriminator: Uint8Array;
  mint: PublicKey;
};

export type UpdatePricesPythInstructionDataArgs = { mint: PublicKey };

export function getUpdatePricesPythInstructionDataSerializer(): Serializer<
  UpdatePricesPythInstructionDataArgs,
  UpdatePricesPythInstructionData
> {
  return mapSerializer<
    UpdatePricesPythInstructionDataArgs,
    any,
    UpdatePricesPythInstructionData
  >(
    struct<UpdatePricesPythInstructionData>(
      [
        ['discriminator', bytes({ size: 8 })],
        ['mint', publicKeySerializer()],
      ],
      { description: 'UpdatePricesPythInstructionData' }
    ),
    (value) => ({
      ...value,
      discriminator: new Uint8Array([8, 44, 70, 27, 3, 53, 187, 230]),
    })
  ) as Serializer<
    UpdatePricesPythInstructionDataArgs,
    UpdatePricesPythInstructionData
  >;
}

// Args.
export type UpdatePricesPythInstructionArgs =
  UpdatePricesPythInstructionDataArgs;

// Instruction.
export function updatePricesPyth(
  context: Pick<Context, 'eddsa' | 'programs'>,
  input: UpdatePricesPythInstructionAccounts & UpdatePricesPythInstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'zodialV2',
    '5E1ikr753b8RQZdtohZAY8wmpjn2hu9dWzrN5xEasmtu'
  );

  // Accounts.
  const resolvedAccounts = {
    market: {
      index: 0,
      isWritable: false as boolean,
      value: input.market ?? null,
    },
    assetRegistry: {
      index: 1,
      isWritable: false as boolean,
      value: input.assetRegistry ?? null,
    },
    priceCache: {
      index: 2,
      isWritable: true as boolean,
      value: input.priceCache ?? null,
    },
    priceUpdate: {
      index: 3,
      isWritable: false as boolean,
      value: input.priceUpdate ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpdatePricesPythInstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.assetRegistry.value) {
    resolvedAccounts.assetRegistry.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([97, 115, 115, 101, 116, 45, 114, 101, 103])
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.market.value)
      ),
    ]);
  }
  if (!resolvedAccounts.priceCache.value) {
    resolvedAccounts.priceCache.value = context.eddsa.findPda(programId, [
      bytes().serialize(
        new Uint8Array([112, 114, 105, 99, 101, 45, 99, 97, 99, 104, 101])
      ),
      publicKeySerializer().serialize(
        expectPublicKey(resolvedAccounts.market.value)
      ),
    ]);
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getUpdatePricesPythInstructionDataSerializer().serialize(
    resolvedArgs as UpdatePricesPythInstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
